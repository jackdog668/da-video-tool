# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules

# Frontend build output
dist
dist-ssr
*.local

# Editor / OS junk
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri / Rust build artifacts — these are HUGE and re-generated on every build
src-tauri/target/
src-tauri/gen/schemas/

# Installer bundles live on GitHub Releases, not in git
src-tauri/target/release/bundle/

# Local staging folder for releases
release/

# External binaries (200MB+ — fetched by scripts/fetch-binaries.ps1)
src-tauri/binaries/yt-dlp-*.exe
src-tauri/binaries/ffmpeg-*.exe

# Secrets (none today — door's locked anyway)
.env
.env.*
*.key
*.pem
